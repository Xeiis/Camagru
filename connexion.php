<?php

header("Content-Type: text/plain");

$login = (isset($_GET["login"])) ? $_GET["login"] : NULL;
$password = (isset($_GET["password"])) ? $_GET["password"] : NULL;

//faire la requete

//retourner oui ou non selon si il a donner les bonnes informations ou non.
?>